import styled from 'styled-components';

/**
 * Styled Components for Weather Map
 * @module WeatherMapStyles
 */

/**
 * Map wrapper with fixed dimensions and border styles
 */
export const MapWrapper = styled.div`
  border-radius: 12px;
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
  height: 100%;
  margin: 0;
  min-height: 400px;
  overflow: hidden;
  width: 100%;
`; 